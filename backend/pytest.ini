[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: tests that require external services (e.g., real Redis); skipped when their fixtures cannot connect.
